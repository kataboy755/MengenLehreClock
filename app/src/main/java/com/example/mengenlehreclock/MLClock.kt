package com.example.mengenlehreclock

import com.example.mengenlehreclock.model.ClockData

class MLClock {
    fun getMLClock(time: String): ClockData {
        val fiveHourRow = null
        val lastHourRow = null
        val fiftyFiveMinuteRow = null
        val lastMinuteRow = null
        return ClockData(fiveHourRow, lastHourRow, fiftyFiveMinuteRow, lastMinuteRow, null)
    }
}